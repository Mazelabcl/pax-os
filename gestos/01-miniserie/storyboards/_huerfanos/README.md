# Storyboards huérfanos

Estos 6 pares (`.md` + `.png`) son storyboards que no están asignados a un capítulo específico todavía. Originalmente nombrados `otros (1-6)`.

## Status

`pending_aldot_approval` — Aldot tiene que decidir, por cada uno:
1. A qué capítulo pertenece → mover a `../cap-N/` con nombre `shot-NN-descripcion.md`
2. Si es experimento descartado → mover a `_archive/storyboards-descartados/`
3. Si es semilla de un capítulo futuro → mantener acá con un nombre más descriptivo

## Para resolver

Abrir el .png de cada uno, leer el .md, y decidir destino. Idealmente revisar de a 1-2 por sesión, no todos juntos.

## Origen

Aparecieron en git status como untracked desde hace varias sesiones. Probablemente fueron generaciones experimentales o pruebas de shots que quedaron sin ubicación. Se mueven acá durante la reorganización 2026-05-22 para que dejen de ensuciar la raíz de `storyboards/`.
